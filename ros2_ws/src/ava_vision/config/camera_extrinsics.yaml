# Static transform: base_link -> camera_optical_frame
#
# AUTO-GENERATED by calibrate_extrinsics on 2026-06-29T18:27:06.
# Solved (Kabsch/SVD) from table markers 1,2,3,4 at their
# measured base_link positions. RMS = 0.95 cm  (per-marker: 1:0.81cm  2:0.83cm  3:1.07cm  4:1.05cm).
# Quaternion (xyzw) = [-0.010206, 0.999684, -0.001962, 0.022895].
# Re-run `ros2 run ava_vision calibrate_extrinsics` if the
# camera is moved, then `colcon build --packages-select ava_vision`.
#
# Loaded by vision_bringup.launch.py -> tf2_ros static_transform_publisher
# (--roll/--pitch/--yaw path; pitch~0/roll~pi for a downward camera => no gimbal lock).

camera_x: -0.008190
camera_y: -0.055384
camera_z: 0.551296
camera_roll: -3.137197
camera_pitch: 0.045752
camera_yaw: -3.121074
